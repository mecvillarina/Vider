namespace Application.Common.Constants
{
    public static class CustomClaimTypes
    {
        public const string WalletName = nameof(WalletName);
        public const string WalletPassword = nameof(WalletPassword);
    }
}
