namespace Application.Common.Constants
{
    public static class SettingKeys
    {
        public const string AzureStorageCdn = nameof(AzureStorageCdn);
        public const string JwtAudience = nameof(JwtAudience);
        public const string JwtIssuer = nameof(JwtIssuer);
        public const string JwtSecret = nameof(JwtSecret);
        public const string AzureStorageAccount = nameof(AzureStorageAccount);
        public const string FaucetEndpoint = nameof(FaucetEndpoint);
        public const string XPRLEndpoint = nameof(XPRLEndpoint);
    }
}
